/*	
	A. Kyoya and Photobooks
	Time limit 2 s
	Memory Limit 256 MB
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
	string s;
	cin >> s;
	printf("%d\n", (s.length()+1)*25 + 1);

	return 0;
}