/*	
	C. Perfect Team
	Time limit 2 s
	Memory Limit 256 MB
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
	int q;
	scanf("%d", &q);
	while (q--) {
		int c,m,x;
		scanf("%d%d%d", &c, &m, &x);
		int nteams = min(c, m);
		
	}

	return 0;
}