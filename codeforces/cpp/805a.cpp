/*	
	A. Fake NP
	Time limit 1 s
	Memory Limit 256 MB
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
	int a,b;
	scanf("%d%d", &a, &b);
	if (b>a)
		printf("2\n");
	else printf("%d\n", a);

	return 0;
}