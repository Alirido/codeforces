/*
	B. Nikita and string
	Time limit 2 s
	Memory Limit 256 MB
*/

#include <bits/stdc++.h>
using namespace std;

int main() {
	double s;
	int len1=0, len2=0; max=0, i=0;
	bool froma=true;
	while (i < s.length()) {
		if (froma) {
			if (s[i] == 'a') {
				len1++;
			} else 
		}
	}